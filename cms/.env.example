# SQLite Database
# Local development: file path relative to CMS directory
# Production: set via docker-compose (file:/app/data/payload.db)
DATABASE_URL=file:./data/payload.db

PAYLOAD_SECRET=change-this-to-a-random-secret-in-production

# SMTP
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
MAIL_FROM=hallo@mens-circle.de

# Admin notifications
ADMIN_EMAIL=

# Frontend URL (for CORS and email links)
SITE_URL=http://localhost:4400

# CMS URL (for admin links in emails)
CMS_URL=http://localhost:3001

# Astro Revalidation (for triggering updates when content changes)
ASTRO_REVALIDATE_URL=http://localhost:4400
REVALIDATE_SECRET=change-this-to-a-random-secret-matching-astro
