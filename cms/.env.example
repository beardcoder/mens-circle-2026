# SQLite Database
# Local development: file path relative to CMS directory
# Production: set via docker-compose (file:/app/data/payload.db)
DATABASE_URL=file:./data/payload.db

PAYLOAD_SECRET=change-this-to-a-random-secret-in-production

# SMTP
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
MAIL_FROM=hallo@mens-circle.de

# Admin notifications
ADMIN_EMAIL=

# Site URL for absolute links (optional, for emails, SEO, etc.)
NEXT_PUBLIC_SITE_URL=http://localhost:3001

# Umami Analytics (optional)
NEXT_PUBLIC_UMAMI_ENABLED=false
NEXT_PUBLIC_UMAMI_WEBSITE_ID=
NEXT_PUBLIC_UMAMI_SCRIPT_URL=https://cloud.umami.is/script.js
