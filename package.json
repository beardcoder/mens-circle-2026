{
  "name": "mens-circle",
  "private": true,
  "workspaces": [
    "cms"
  ],
  "scripts": {
    "dev": "npm run --workspace=cms dev",
    "build": "npm run --workspace=cms build",
    "start": "npm run --workspace=cms start",
    "lint:prettier:fix": "prettier . --write"
  },
  "dependencies": {
    "prettier": "^3.8.1"
  }
}
